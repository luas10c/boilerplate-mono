/** @type{import('prettier').Config} */
const config = {
  semi: false,
  singleQuote: true,
  trailingComma: 'none',
  endOfLine: 'lf',
  bracketSpacing: true,
  arrowParens: 'always',
  printWidth: 80,
  tabWidth: 2,
  useTabs: false
}

export default config
